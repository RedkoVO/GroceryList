export interface IProduct {
  id: string
  title: string
  count: number
  isDone: boolean
}

export type ProductDataT = {
  title: string
  count: number
  isDone: boolean
}

export type CounterDataT = {
  id: string
  count: number
}

export type CheckboxDataT = {
  id: string
  isDone: boolean
}

export type TitleDataT = {
  id: string
  title: string
}
